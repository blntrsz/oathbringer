[Unit]
Description=OpenCode Server

[Service]
ExecStart=/bin/sh -c 'OPENCODE_SERVER_PASSWORD=$(cat "$CREDENTIALS_DIRECTORY/opencode_password") exec /home/thdxr/.bun/bin/opencode serve --hostname 0.0.0.0 --port 4096'
LoadCredential=opencode_password:/home/thdxr/.config/opencode/credentials/server_password
Restart=on-failure

[Install]
WantedBy=default.target
